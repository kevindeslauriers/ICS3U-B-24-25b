package day1;

public class ForLoopExamples {
    public static void main(String[] args) {
        // use a loop to repeat a section of code

        // for loop is meant to repeat code a specific number of times
        // meant to countr and has a counter built in

        // for (int i = 0; i < 10; i++) {
        //     System.out.println(i);
        // }

        // int i = 0; defines the counter and sets it
        // i < 10; is the condition if true it does the body
        // i++ is how it counts

        // print -10 -8 -6 -4 -2 0 2 4
        // no if statements allowed

        // for (int i = -10; i < 5; i+=2) {
        //     System.out.println(i);
        // }

        // print 1000 to -1000

        // for (int i = 1000; i > -1001 ; i--) {
        //     System.out.println(i);
        // }

        // sum the numbers from 1 to 1000

        // int sum = 0;
        // for (int soup = 1; soup < 1001; soup++) {
        //     sum += soup;
        // }

        // System.out.println(sum);

        // String str = "Alphabet Soup";

        // for (int index = str.length() - 1; index >= 0; index--) {
        //     System.out.print(str.substring(index, index+1));
        // }

        // System.out.println();

        // count the number of vowels
        // String str = "Alphabet Soup";


        // int numVowels = 0;
        // for (int i = 0; i < str.length(); i++) {
        //     String letter = str.substring(i, i+1);

        //     if ("aeiouAEIOU".indexOf(letter)>= 0){
        //         numVowels++;
        //     }
        // }

        // System.out.println(numVowels);
    }

}